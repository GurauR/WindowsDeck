<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Deck" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:actionBarStyle">@android:style/Theme.Material.NoActionBar</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/design_default_color_secondary</item>
    </style>

    <style name="MyMaterialTheme" parent="Theme.AppCompat.DayNight">
        <!-- Add attributes here -->
        <item name="colorPrimary">@color/black</item>
    </style>

</resources>